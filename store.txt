<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Website</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-Jz2qnjpt1DEKCvItzjw3N8A/qYtVxJS+O30Kd4k6PBmsuTLl7Fmx1z2SuvlYYn1adQn1DYYtGrc2GQ4TXus6mQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
    <div class="container">
        <section class="hero">
            <header>
                <h1>Photo Website</h1>
                <button id="mode-toggle" onclick="toggleMode()">Change Mode</button>
            </header>
            <div class="hero-content">
                <h2>Stunning Free Images &amp; Royalty-Free Stock</h2>
                <p>Over 1 million+ high-quality stock images and videos shared by our talented community.</p>
                <div class="search-box">
                    <input type="text" id="search-input" placeholder="Search for photos...">
                    <button onclick="searchPhotos()">Search</button>
                </div>
            </div>
        </section>
        <section class="top-keywords">
            <h3>Top Searches</h3>
            <div class="keywords">
                <button onclick="searchKeyword('animal')">Animal</button>
                <button onclick="searchKeyword('nature')">Nature</button>
                <button onclick="searchKeyword('mountain')">Mountain</button>
                <button onclick="searchKeyword('dog')">Dog</button>
                <button onclick="searchKeyword('cat')">Cat</button>
            </div>
        </section>

        <div id="photo-gallery" class="photo-gallery"></div>
    </div>
    <script src="data.js"></script>
    <script src="script.js"></script>
</body>
</html>

























































































:root {
    --background-light: #f4f4f4;
    --text-light: #000;
    --background-dark: #121212;
    --text-dark: #fff;
    --primary-color: #6200ea;
    --secondary-color: #03dac6;
    --button-hover: #3700b3;
}

body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    text-align: center;
    background-color: var(--background-light);
    color: var(--text-light);
    transition: background-color 0.3s, color 0.3s;
    overflow-x: hidden;
}

.container {
    max-width: 100%;
    margin: 0 0;
    padding: 0 20px;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    padding: 20px;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 2;
    background: rgba(0, 0, 0, 0.315);/* Semi-transparent background */
    box-sizing: border-box;
}





h1 {
    font-size: 2.5em;
    margin: 0;
    color: var(--primary-color);
}

.hero {
    background-image: url('https://cdn.pixabay.com/photo/2024/06/21/15/18/road-8844555_1280.jpg'); /* Replace with your background image URL */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    height: 75vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: #fff;
    position: relative;
    text-align: center;
    box-sizing: border-box;
    padding: 20px;
}

.hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5); /* Dark overlay */
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2;
}

.hero h2 {
    font-size: 3em;
    margin-bottom: 20px;
}

.hero p {
    font-size: 1.5em;
    margin-bottom: 20px;
}

.search-box {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
    box-sizing: border-box;
}

.search-box input {
    padding: 10px 15px;
    font-size: 1em;
    width: 80%;
    max-width: 400px;
    border: 2px solid var(--primary-color);
    border-radius: 4px 0 0 4px;
    outline: none;
    transition: border-color 0.3s;
    box-sizing: border-box;
}

.search-box input:focus {
    border-color: var(--secondary-color);
}

.search-box button {
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    background-color: var(--secondary-color);
    color: #fff;
    border: none;
    border-radius: 0 4px 4px 0;
    transition: background-color 0.3s;
    box-sizing: border-box;
}

.search-box button:hover {
    background-color: var(--button-hover);
}

.top-keywords {
    margin-top: 30px;
    margin-bottom: 70px;
}

.top-keywords h3 {
    font-size: 1.5em;
    margin-bottom: 10px;
}

.keywords {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
}

.keywords button {
    padding: 10px 15px;
    font-size: 1em;
    margin: 5px;
    cursor: pointer;
    background-color: blueviolet;
    color: #fff;
    border: none;
    border-radius: 4px;
    transition: background-color 0.3s;
}

.keywords button:hover {
    background-color: var(--button-hover);
}

#mode-toggle {
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    background-color: var(--primary-color);
    color: #fff;
    border: none;
    border-radius: 4px;
    transition: background-color 0.3s;
}

#mode-toggle:hover {
    background-color: var(--button-hover);
}

.photo-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 10px;
    justify-items: center;
}

.photo-gallery img {
    width: 100%;
    height: auto;
    border: 1px solid #ddd;
    border-radius: 4px;
    transition: transform 0.2s;
}

.photo-gallery img:hover {
    transform: scale(1.05);
}

body.dark-mode {
    background-color: var(--background-dark);
    color: var(--text-dark);
}

body.dark-mode .search-box input {
    border-color: var(--primary-color);
}

body.dark-mode .search-box input:focus {
    border-color: var(--secondary-color);
}

body.dark-mode .search-box button, 
body.dark-mode #mode-toggle {
    background-color: var(--primary-color);
    color: #fff;
}

body.dark-mode .search-box button:hover,
body.dark-mode #mode-toggle:hover {
    background-color: var(--button-hover);
}

body.dark-mode .photo-gallery img {
    border: 1px solid #444;
}














.modal {
    display: none;
    position: fixed;
    z-index: 1;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgb(0,0,0);
    background-color: rgba(0,0,0,0.9);
}

.modal-content {
    margin: auto;
    display: block;
    width: 80%;
    max-width: 700px;
    max-height: 80%;
}

.modal-footer {
    background-color: #fefefe;
    text-align: center;
    padding: 10px;
}

.close {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
}

.close:hover,
.close:focus {
    color: black;
    text-decoration: none;
    cursor: pointer;
}
